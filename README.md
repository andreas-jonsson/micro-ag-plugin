Provides a `ag` command in micro to search for content in all project files using The Silver Searcher.
It also provides a `fzf` command for quickly open files.

Requires `ag`, `fzf` and `bash` to be installed.
